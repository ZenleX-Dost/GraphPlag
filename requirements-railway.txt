# Railway-specific minimal requirements
# Optimized for deployment (excludes dev tools, visualization libs)

# Pin NumPy to 1.x for GraKeL compatibility
numpy>=1.21.0,<2.0.0

# Core dependencies
scipy>=1.7.0

# NLP Processing (minimal set)
spacy>=3.5.0
sentence-transformers>=2.2.0
transformers>=4.30.0
langdetect>=1.0.9

# PyTorch (CPU-only to save space)
--index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0

# Graph Processing
networkx>=3.0
grakel>=0.1.9

# Machine Learning
scikit-learn>=1.0.0

# Utilities (minimal)
pyyaml>=6.0
tqdm>=4.64.0

# File Parsing
PyPDF2>=3.0.0
python-docx>=1.0.0
markdown>=3.4.0

# API Dependencies
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6
reportlab>=4.0.8
openpyxl>=3.1.2
python-dotenv>=1.0.0

# Minimal utilities
psutil>=5.9.0

# EXCLUDED for size optimization:
# - stanza (large NLP models)
# - torch-geometric (not needed for API)
# - wandb, tensorboard (experiment tracking)
# - gradio (UI framework)
# - matplotlib, plotly, seaborn, pyvis (visualization)
# - pandas (not essential for API)
# - pytest, black, flake8, mypy (dev tools)

